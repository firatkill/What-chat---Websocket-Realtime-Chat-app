spring.application.name=what-chat
# Uygulaman?n çal??aca?? port numaras? (varsay?lan: 8080)

server.port=8080


# Enable/Disable the generation of OpenAPI documentation (default: true)
springdoc.api-docs.enabled=true

# Enable/Disable Swagger UI (default: true)
springdoc.swagger-ui.enabled=true

# Custom path for the Swagger UI (default: /swagger-ui.html)
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.enable-spring-security=true


# Default response content type for API endpoints
springdoc.default-produces-media-type=application/json

# Default request content type for API endpoints
springdoc.default-consumes-media-type=application/json

# ??????????? VER?TABANI AYARLARI ???????????

# PostgreSQL veritaban? ba?lant? URL?si (veritaban? ad?: what_chat)

spring.datasource.url=jdbc:postgresql://localhost:5432/what_chat

# PostgreSQL kullan?c? ad? (kendi kullan?c? ad?n?zla de?i?tirin)

spring.datasource.username=firatkil

# PostgreSQL parolas? (kendi parolan?zla de?i?tirin)

spring.datasource.password=1234

# ??????????? JPA / HIBERNATE AYARLARI ???????????

# Hibernate?e veritaban? ?emas?n? otomatik olarak güncellemesini söyler (dev ortam? için uygundur)

spring.jpa.hibernate.ddl-auto=update

# Çal??an SQL sorgular?n?n konsola yazd?r?lmas?n? sa?lar (geli?tirme ve debug amaçl?)

spring.jpa.show-sql=false

# ??????????? JWT AYARLARI ???????????

# JWT (JSON Web Token) için kullan?lacak gizli anahtar

app.jwt.secret=YourSuperSecretJWTKeyWithAtLeast256Bits!

# JWT token??n?n geçerlilik süresi (ms cinsinden), burada 1 gün = 86400000 ms

app.jwt.expiration-ms=86400000